# Environment Configuration for German Insurance Backend
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ============================================
# Server Configuration
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# Supabase Configuration (Required)
# ============================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key-here

# Note: Use anon key for basic operations, service role key for admin operations
# SUPABASE_SERVICE_KEY=your-service-role-key-here

# ============================================
# Vertex AI Configuration (Required for Production)
# ============================================
# Google Cloud Project Configuration
VERTEX_AI_PROJECT_ID=your-gcp-project-id
VERTEX_AI_LOCATION=europe-west3

# Embedding Model Configuration
VERTEX_AI_EMBEDDING_MODEL=textembedding-gecko@003
VERTEX_AI_EMBEDDING_DIMENSION=768

# LLM Model Configuration
VERTEX_AI_LLM_MODEL=gemini-1.5-flash
VERTEX_AI_LLM_TEMPERATURE=0.7
VERTEX_AI_LLM_MAX_TOKENS=2048

# Authentication (choose one method)
# Method 1: Service Account Key File (recommended for local development)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Method 2: Service Account Key JSON (for cloud deployment)
# GOOGLE_CLOUD_CREDENTIALS={"type":"service_account","project_id":"..."}

# ============================================
# RAG Configuration
# ============================================
# Vector Search Settings
SIMILARITY_THRESHOLD=0.7
MAX_RESULTS=5

# Chunking Settings (for data processing)
CHUNK_SIZE=800
CHUNK_OVERLAP=75

# ============================================
# API Rate Limiting & Timeouts
# ============================================
# Request timeout in milliseconds
TIMEOUT_MS=30000

# Max retries for failed API calls
MAX_RETRIES=3

# Retry delay (exponential backoff starting point)
RETRY_DELAY_MS=1000

# ============================================
# Logging Configuration
# ============================================
# Options: error, warn, info, debug
LOG_LEVEL=info

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,https://your-frontend.com

# ============================================
# Development/Testing
# ============================================
# Mock API responses (for testing without real APIs)
# MOCK_VERTEX_AI=true
# MOCK_EMBEDDINGS=true
